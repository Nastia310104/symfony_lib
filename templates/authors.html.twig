{% extends 'layouts/layout.html.twig' %}

{% block main %}
    <div class="container-fluid mt-4">
            <table class="table table-dark table-striped">
                <thead>
                <tr>
                    <th>Name (number of books)</th>
                    <th>Book's name</th>
                    <th>Book's year</th>
                </tr>
                </thead>
                <tbody>
                </tbody>
            </table>

        </div>
    </div>


{% endblock %}